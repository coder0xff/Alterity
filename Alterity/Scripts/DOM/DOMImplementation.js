define(function () {
    var instance = null;
    instance.CreateHTMLDocument(title)
    {
        var result = new (require("Document"))();
        result.Create
    }

    function DOMImplementation() {
        if (instance != null) return instance;
        this.hasFeature = function (feature, version) {
            return false;
        }
    };

    return DOMImplementation;
});